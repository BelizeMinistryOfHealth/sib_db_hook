test:
  DbUsername: postgres
  DbPassword: postgres
  DbDatabase: postgres
  DbHost: localhost
  DbType: postgres
  ApiToken: "token"

dev:
  DbUsername: postgres
  DbPassword: postgres
  DbDatabase: postgres
  DbHost: localhost
  DbType: postgres
  ApiToken: "token"